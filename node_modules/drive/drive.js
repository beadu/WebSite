var unload = require('unload');

exports.start = function (bus) {
    if (bus.engine === null)
        unload.exec({ code: 404, msg: '没有找到您需要的服务！' });

    if (bus.req.method === 'GET')
        bus.engine.get(bus);
    else if (bus.req.method === 'POST')
        bus.engine.post(bus);
};